1. CREATE DATABASE asset_management;
2. USE asset_management;
3. CREATE TABLE employee (
    employee_id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    gender ENUM('Male', 'Female', 'Other') NOT NULL,
    contact_number VARCHAR(15) NOT NULL,
    address VARCHAR(255) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    role ENUM('Admin', 'User') NOT NULL
);
4. CREATE TABLE asset (
    asset_id INT AUTO_INCREMENT PRIMARY KEY,
    asset_name VARCHAR(100) NOT NULL,
    asset_category VARCHAR(50) NOT NULL,
    asset_model VARCHAR(50),
    manufacturing_date DATE,
    expiry_date DATE,
    asset_value DECIMAL(10, 2) NOT NULL,
    status ENUM('Available', 'Borrowed') DEFAULT 'Available'
);
5. CREATE TABLE asset_borrowing (
    borrowing_id INT AUTO_INCREMENT PRIMARY KEY,
    employee_id INT,
    asset_id INT,
    borrowed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    returned_at TIMESTAMP,
    status ENUM('Active', 'Returned') DEFAULT 'Active',
    FOREIGN KEY (employee_id) REFERENCES employee(employee_id)
        ON DELETE CASCADE
        ON UPDATE CASCADE,
    FOREIGN KEY (asset_id) REFERENCES asset(asset_id)
        ON DELETE CASCADE
        ON UPDATE CASCADE
);
6. CREATE TABLE service_request (
    service_request_id INT AUTO_INCREMENT PRIMARY KEY,
    employee_id INT,
    asset_id INT,
    description TEXT NOT NULL,
    issue_type ENUM('Hardware Failure', 'Software Issue', 'Routine Maintenance') NOT NULL,
    status ENUM('Pending', 'In Progress', 'Completed') DEFAULT 'Pending',
    requested_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (employee_id) REFERENCES employee(employee_id)
        ON DELETE CASCADE
        ON UPDATE CASCADE,
    FOREIGN KEY (asset_id) REFERENCES asset(asset_id)
        ON DELETE CASCADE
        ON UPDATE CASCADE
);
7. CREATE TABLE asset_audit (
    audit_id INT AUTO_INCREMENT PRIMARY KEY,
    employee_id INT,
    asset_id INT,
    audit_status ENUM('Pending', 'Verified', 'Rejected') DEFAULT 'Pending',
    requested_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (employee_id) REFERENCES employee(employee_id)
        ON DELETE CASCADE
        ON UPDATE CASCADE,
    FOREIGN KEY (asset_id) REFERENCES asset(asset_id)
        ON DELETE CASCADE
        ON UPDATE CASCADE
);

8. CREATE TABLE `asset_management`.`role` (
  `role_id` INT NOT NULL AUTO_INCREMENT,
  `role_name` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`role_id`),
  UNIQUE INDEX `role_name_UNIQUE` (`role_name` ASC) VISIBLE);

9.ALTER TABLE `asset_management`.`employee` 
DROP COLUMN `role`,
ADD COLUMN `role_id` INT NOT NULL AFTER `password`,
ADD INDEX `role_id_idx` (`role_id` ASC) VISIBLE;
;
ALTER TABLE `asset_management`.`employee` 
ADD CONSTRAINT `role_id`
  FOREIGN KEY (`role_id`)
  REFERENCES `asset_management`.`role` (`role_id`)
  ON DELETE RESTRICT
  ON UPDATE CASCADE;

10. CREATE TABLE `asset_management`.`asset_category` (
  `category_id` INT NOT NULL AUTO_INCREMENT,
  `category_name` VARCHAR(100) NOT NULL,
  PRIMARY KEY (`category_id`),
  UNIQUE INDEX `category_name_UNIQUE` (`category_name` ASC) VISIBLE);

11. ALTER TABLE `asset_management`.`asset` 
CHANGE COLUMN `asset_category` `category_id` INT NOT NULL ,
ADD INDEX `category_id_idx` (`category_id` ASC) VISIBLE;
;
ALTER TABLE `asset_management`.`asset` 
ADD CONSTRAINT `category_id`
  FOREIGN KEY (`category_id`)
  REFERENCES `asset_management`.`asset_category` (`category_id`)
  ON DELETE RESTRICT
  ON UPDATE CASCADE;

12. CREATE TABLE `asset_management`.`issue_type` (
  `issue_type_id` INT NOT NULL AUTO_INCREMENT,
  `issue_type_name` VARCHAR(100) NOT NULL,
  PRIMARY KEY (`issue_type_id`),
  UNIQUE INDEX `issue_type_name_UNIQUE` (`issue_type_name` ASC) VISIBLE);

13. ALTER TABLE `asset_management`.`service_request` 
CHANGE COLUMN `issue_type` `issue_type_id` INT NOT NULL ,
ADD INDEX `issue_type_id_idx` (`issue_type_id` ASC) VISIBLE;
;
ALTER TABLE `asset_management`.`service_request` 
ADD CONSTRAINT `issue_type_id`
  FOREIGN KEY (`issue_type_id`)
  REFERENCES `asset_management`.`issue_type` (`issue_type_id`)
  ON DELETE RESTRICT
  ON UPDATE CASCADE;

14. 